export default function Footer() {
  return (
    <footer className="border-t border-reel-line/60 py-10">
      <div className="sprocket-rail mb-8" />
      <div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-4 px-6 text-sm text-reel-mist sm:flex-row">
        <p>Reelfetch — for personal use with content you have the rights to.</p>
        <p>Built for the reel, not the archive. Files aren't stored on our servers.</p>
      </div>
    </footer>
  );
}
